<% title_tag_set("会員一覧")%>

<h1>会員一覧</h1>
<%= paginate @users %>
<div class='users'>
  <% @users.each do |user| %>
  <div class="user">
    <%= link_to user.name, user%>
  </div>
  <% end %>
</div>
<%= paginate @users %>
